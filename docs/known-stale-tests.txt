# 已知陈旧测试白名单（不阻塞 /tke-release skill 的 P1 测试检查）
#
# 每行一个测试名（不含包路径），形如 TestXxxYyy
# 跑 `go test` 时 grep "^--- FAIL: <name>" 命中本文件的行会被忽略
# 进入此白名单需满足：HEAD~1 也失败（非本次引入）+ 已记录原因
#
# 2026-06-16 加入：
#   TestListModelsTokenLimitIncludesTieredBillingModel
#     原因: 测试直接调 controller.ListModels → 触发 GetUserGroup → cacheGetUserBase
#           → common.RedisHGetObj(nil)，本地无 Redis mock 必 panic
#     待办: 加 Redis mock 或在 cacheGetUserBase 内做 nil-guard

TestListModelsTokenLimitIncludesTieredBillingModel
