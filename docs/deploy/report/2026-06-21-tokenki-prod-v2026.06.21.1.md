# 发布报告 — tokenki v2026.06.21.1

> 环境:prod · 集群:cls-tokensolo-tke-japan / ns tokenki · 时间:2026-06-21 23:17 (Asia/Shanghai)

## 概览
| 项 | 值 |
|----|----|
| 服务 | tokenki |
| 环境 | prod |
| 版本 tag | v2026.06.21.1 |
| 镜像 | ghcr.io/heiying0917/tokenki:v2026.06.21.1 |
| 发布结果 | ✅ 成功 |
| 本次内容 | 渠道管理:成本价表头点击升/降序排序(后端 sort 选项 + classic 前端接线) |

## 各阶段结果
| 阶段 | 状态 | 详情 |
|------|------|------|
| 合并 main | ✅ | merge 拉取 origin `a4d66cec`(.claude/settings.json +5),与功能改动无重叠,无冲突 |
| commit | ✅ | `254f142c` feat(channel) 成本价排序;`8fbfd39a` docs(WORKLOG+上轮报告);`32ccae26` merge |
| 打 tag & push | ✅ | v2026.06.21.1(指向 32ccae26) |
| 镜像构建 (Actions) | ✅ | run-id 27908439741,completed/success(JSON 二次校验) |
| 集群部署 | ✅ | master 先滚 → slave 后滚,均 successfully rolled out |
| 健康检查 | ✅ | https://tokenki.com/api/status → HTTP 200 + success:true(system_name=TokenKi) |
| 冒烟测试 | ✅(含已知 token 限制) | status✅;relay 真实补全因 smoke token 组 `default` 无渠道返回 model_not_found(pre-existing,与上版一致);本功能 SQL 在生产 PG 验证通过 |

## 部署详情
| Deployment | 旧镜像 | 新镜像 | rollout |
|-----------|--------|--------|---------|
| tokenki-api-master | v2026.06.18.2 | v2026.06.21.1 | ✅ successfully rolled out |
| tokenki-api-slave  | v2026.06.18.2 | v2026.06.21.1 | ✅ successfully rolled out |

## 验证
| 检查项 | 结果 |
|--------|------|
| Pod 全 Running | ✅ master 1/1 + slave 2/2(0 重启) |
| 镜像 tag 已更新 | ✅ master/slave 均 v2026.06.21.1 |
| 渠道缓存加载 | ✅ "channels synced from database" 每 60s 正常 |
| 日志无 panic | ✅ master+slave tail 扫描无 panic/nil pointer/runtime error |
| API 冒烟 | ✅ /api/status 200;relay 见下方说明 |
| 本功能(成本价排序) | ✅ 生产 PG `ORDER BY COALESCE(cost_price,0)` 升/降序正确(升:11→13,降反之),0/NULL 落便宜端;后端 3 个 TDD 单测 + model 全包 100 passed |
| 部署后 5xx(CLS) | ✅ 仅自测 4×503(user_id 2,model_not_found),真实用户 5xx = 0 |

### 冒烟测试 relay 说明(非回归)
- smoke token(user_id 2)属 group `default`,而所有启用渠道在 `claude官key`/`claude速刷`/`OpenAI官key`/`Claude官key` 等组,group `default` 无任何渠道 → 换任何模型(含已配置的 claude-3-5-sonnet-20241022 / gpt-5.4-mini)均返回 `model_not_found / No available channel under group default`。
- 此为 token 组配置现状,与上一轮 v2026.06.18.2 一致(用户已确认),非本次部署引入。
- 本次改动仅触达 admin 渠道列表排序路径(`model/channel.go` `Apply()`),不触达 relay 派发路径(`GetRandomSatisfiedChannel`/channel_cache);派发基础设施经 "channels synced" + distributor 正常返回 model_not_found(非崩溃)验证健康。
- 未覆盖:本功能 admin HTTP 端点 + 前端点击需管理员登录态,建议浏览器验收(前端已嵌入镜像,Dockerfile builder-classic 阶段 `bun run build`)。

## 回滚记录
无。

## 变更文件(254f142c)
- `model/channel.go`:channelSortColumns 加 cost_price;Apply() 用 `COALESCE(cost_price,0) ASC|DESC`(三库一致,Rule 2)
- `model/channel_cost_sort_test.go`:新增 TDD 测试(接受性/升降序/NULL 落位)
- `web/classic/src/hooks/channels/useChannelsData.jsx`:sortBy/sortOrder + handleSortChange,sort_by/sort_order 拼入请求(标签聚合模式不下发)
- `web/classic/src/components/table/channels/ChannelsColumnDefs.jsx`:成本价列 sorter:true
- `web/classic/src/components/table/channels/ChannelsTable.jsx`:CardTable onChange(升/降/三击清除)
