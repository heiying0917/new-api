# 发布报告 — tokenki v2026.06.16.2

> 环境：prod · 集群：cls-tokensolo-tke-japan / ns tokenki · 时间：2026-06-16 17:46

## 概览

| 项 | 值 |
|----|----|
| 服务 | tokenki |
| 版本 tag | v2026.06.16.2 |
| 镜像 | ghcr.io/heiying0917/tokenki:v2026.06.16.2 |
| 核心改动 | logger 全面 NDJSON 化（含 Gin access log + GORM logger） |
| 发布结果 | ✅ 成功，零停机 |

## 关键 commits

```
c57ff94c chore(ci): 清理 7 个对 tokenki 无用的上游 workflow
e694a713 chore(test): 加入陈旧测试白名单（known-stale-tests.txt）
ab295d85 feat(logger): stdout 日志统一改成 NDJSON（含 access log + GORM）
```

## 各阶段结果

| 阶段 | 状态 | 详情 |
|------|------|------|
| P1 前置检查 | ✅ | 工作区已 commit 白名单，go build PASS，go test 仅陈旧测试白名单失败（HEAD~1 同样失败，非本次引入） |
| P2 镜像构建 | ✅ | GHCR build run 27608492579，耗时 ~5min（amd64-only） |
| P3 集群部署 | ✅ | master 1→1 + slave 2→2 Running，rollout 顺序 master → slave，无 CrashLoopBackOff |
| P4 冒烟测试 | ✅ | /api/status 200 + success:true；access log JSON 在生产验证生效 |
| P5 事故响应 | ⬜ | 未触发 |
| P6 部署后监控 | ✅ | 5 分钟巡检：0 panic / 0 5xx / 0 4xx |

## 部署详情

| Deployment | 旧镜像 | 新镜像 | rollout |
|-----------|--------|--------|---------|
| tokenki-api-master | v2026.06.16.1 | v2026.06.16.2 | ✅ |
| tokenki-api-slave | v2026.06.16.1 | v2026.06.16.2 | ✅ |

## 验证

| 检查项 | 结果 |
|--------|------|
| Pod 全 Running | ✅ master 1/1 + slave 2/2 |
| 镜像 tag 已更新 | ✅ |
| 启动日志无 panic | ✅ |
| **stdout NDJSON 生效** | ✅ 所有 `{"ts":...,"service":"tokenki","msg":...}` 格式 |
| **Access log JSON 生效** | ✅ type=access + status/latency_ms/method/path/client_ip/route_tag/user_id 全字段 |
| **GORM SQL JSON 生效** | ✅ 慢 SQL warning 也是 single-line JSON（type=sql/elapsed_ms） |
| **旧 [GIN]/[SYS] 文本格式残留** | ✅ 无（100% NDJSON） |
| API 冒烟 | ✅ /api/status；模型 chat/anthropic 因 admin 未配渠道返回 model_not_found 503（pre-existing） |

## 冒烟测试详情

| 场景 | 请求 | 实际响应 | HTTP | 结论 |
|------|------|---------|------|------|
| 1. /api/status | GET | success:true | 200 | ✅ |
| 2. OpenAI chat | gpt-4o-mini | model_not_found | 503 | ⚠️ pre-existing |
| 3. Anthropic | claude-sonnet-4-6 | model_not_found | 503 | ⚠️ pre-existing |
| 4. List models | GET /v1/models | data:[] | 200 | ✅ |

**未覆盖**：relay path 实际 LLM 调用（admin 未配渠道导致）。建议 admin 配 1 个渠道后用相同 token 跑一次完整 relay 验证。

## 回滚记录

无。

## 后续 TODO

1. **腾讯云 CLS 索引配置**：本次发版后 stdout 已全 NDJSON。CLS 主题 `tke_tokenki` 需从"单行-全文"提取模式改为"JSON"提取模式，并开键值索引（status/level/type/service/path 等字段），P6 监控 skill 的 SQL 查询才能跑起来。详见 `docs/deploy/plan/cls-json-index.md`。
2. **GHCR PAT 轮换**：首次部署时 PAT 因 base64 错误暴露在过 shell 输出，应轮换。
3. **admin 配渠道**：让冒烟测试能跑通完整 relay path。
4. **陈旧测试修复**：给 `TestListModelsTokenLimitIncludesTieredBillingModel` 加 Redis mock，移出白名单。
