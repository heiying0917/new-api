# 发布报告 — tokenki v2026.09.17.2（v2026.09.17.1 回滚后的修复重发）

> 环境:prod · 集群:cls-tokensolo-tke-japan / ns tokenki · 时间:2026-09-17 14:02 (UTC+8)

## 概览
| 项 | 值 |
|----|----|
| 服务 | tokenki |
| 环境 | prod |
| 版本 tag | v2026.09.17.2 |
| 镜像 | ghcr.io/heiying0917/tokenki:v2026.09.17.2 |
| 发布结果 | ✅ 成功 |
| 本次内容 | v2026.09.17.1 全部内容（上游安全/稳定性修复 + Claude 5 / GPT-5.6 模型）+ 修复 `8d0fb08f0`：json 列 Valuer 返回 string（上游 6eb6f35ed），解决 PG simple protocol 下建/改渠道 `SQLSTATE 22P02`；`0a58ec146`：PG 写路径冒烟脚本 + SOP 加固 + 事故复盘 |

## 各阶段结果
| 阶段 | 状态 | 详情 |
|------|------|------|
| 预检 | ✅ | `go build ./...`；`go test ./model/ ./controller/` 仅白名单 `TestListModelsTokenLimitIncludesTieredBillingModel`；上游回归测试 `model/json_column_test.go` 修复前 2 FAIL → 修复后 PASS（反向验证） |
| **PG 写路径冒烟（新增 Step 1.1b）** | ✅ | `pg-write-smoke.sh`：一次性 postgres:15 + 待发版二进制 → setup/login → 建渠道 INSERT(json) ✅ → 改渠道 UPDATE(json) ✅ → 读回 Scan ✅ → 日志 22P02/panic = 0；同脚本对修复前二进制稳定复现 22P02 |
| 打 tag & push | ✅ | main `429d231e1..0a58ec146` push；tag v2026.09.17.2 → 0a58ec146 |
| 镜像构建 (Actions) | ✅ | run-id 35187681188，watch exit 0 + JSON 二次校验 completed/success，约 5 分钟；GHCR manifest linux/amd64 |
| 集群部署 | ✅ | master×1 → slave×2 顺序滚动成功，零停机；完成 06:02:48Z |
| 健康检查 | ✅ | https://tokenki.com/api/status → 200，success:true，system_name=TokenKi |
| 冒烟测试 | ✅ | 见下表；0 panic |
| 部署后监控 | ✅ | P6 六轮（06:03:46Z–06:06:41Z 累计）：真实用户 5xx = 0，**22P02 = 0，channels 写入 SQL error = 0** |

## 部署详情
| Deployment | 旧镜像 | 新镜像 | rollout |
|-----------|--------|--------|---------|
| tokenki-api-master | v2026.06.24.1（回滚态） | v2026.09.17.2 | ✅ successfully rolled out |
| tokenki-api-slave  | v2026.06.24.1（回滚态） | v2026.09.17.2 | ✅ successfully rolled out |

## 验证
| 检查项 | 结果 |
|--------|------|
| Pod 全 Running | ✅ master 1/1 + slave 2/2，0 重启，旧 Pod 已随 grace period 退出 |
| 镜像 tag 已更新 | ✅ 三个 Pod 均 v2026.09.17.2 |
| 日志无 panic | ✅ master/slave `--tail=300` panic/nil pointer/runtime error = 0，error 级 = 0，22P02 = 0；迁移日志无 error |
| 修复点验证 | ✅ 本地真 PG 端到端（建/改/读渠道）通过；生产 P6 窗口 22P02 与 channels 写入错误均为 0。**生产端到端建渠道待用户在界面重试确认**（需管理员/供应商会话） |

## 冒烟测试报告
| 场景 | 请求模型/接口 | 预期 | 实际响应 | HTTP | 结论 |
|------|-------------|------|---------|------|------|
| 健康检查 | GET /api/status | 200 success:true | success:true | 200 | ✅ |
| 基线 OpenAI chat | gpt-4o-mini | 200 或结构化 model_not_found | model_not_found（冒烟组无渠道） | 503 | ✅ 与历次一致 |
| 基线 Anthropic | /v1/messages claude-sonnet-4-6 | 同上 | model_not_found | 503 | ✅ |
| 本次: Claude 5 effort 后缀 | claude-opus-5-high | 结构化响应 | model_not_found | 503 | ✅ 无 panic |
| 本次: 新模型 | gpt-5.6-sol | 结构化响应 | model_not_found | 503 | ✅ |
| 本次: 渠道新建/编辑（22P02 修复点） | POST/PUT /api/channel/ | success:true | 本地真 PG 端到端 ✅；生产待用户界面重试 | — | ⚠️ 生产待确认 |

**未覆盖场景**：relay 校验/转换/计费路径（冒烟 token 组无渠道）；GHSA-j6gc 的 PUT /api/user/self（需控制台会话）——均由单测覆盖，同 v2026.09.17.1 报告。

## 部署后监控(P6，约 4 分钟)
- 起点 FROM=1789624968000（06:02:48Z），累计窗口，tccli SQL 聚合 CLS。
- 六轮恒为：access 5xx = `503×4`（全部 user_id=2 冒烟：/v1/chat/completions×3、/v1/messages×1）；error 级日志 = 这 4 次请求对应的 distributor 报错；`"22P02"` = 0；`level:error AND type:sql AND "channels"` = 0。
- 结论：修复生效（无 22P02），无版本引入异常，无需回滚。

## 回滚记录
无（本次发版成功）。前序 v2026.09.17.1 的回滚见其报告与 `docs/report/2026-09-17-pg-json-column-22p02.md`。
