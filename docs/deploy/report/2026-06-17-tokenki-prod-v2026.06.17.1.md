# 发布报告 — tokenki v2026.06.17.1

> 环境:prod · 集群:cls-tokensolo-tke-japan / ns tokenki · 时间:2026-06-17 03:02（部署）/ 09:59（监控复核）

## 概览
| 项 | 值 |
|----|----|
| 服务 | tokenki |
| 环境 | prod |
| 版本 tag | v2026.06.17.1 |
| 镜像 | ghcr.io/heiying0917/tokenki:v2026.06.17.1 |
| 本次内容 | feat(supplier) 第十版 — 查看秘钥2FA门禁 / 日志应付供应商金额 / 申请结算确认弹窗 / 管理员重置2FA |
| 发布结果 | ✅ 成功（无回滚） |

## 各阶段结果
| 阶段 | 状态 | 详情 |
|------|------|------|
| 合并 main | ✅ | up-to-date，无冲突 |
| commit | ✅ | 无新提交；本地领先 1 个已有 commit `9d66952d`，发版前 push 到 origin/main |
| 打 tag & push | ✅ | v2026.06.17.1 |
| 镜像构建 (Actions) | ✅ | run-id 27640685553，耗时 5m6s，watch + JSON 二次校验均 completed/success |
| 集群部署 | ✅ | master→收敛→slave 平滑滚动，零停机 |
| 健康检查 | ✅ | https://tokenki.com/api/status → HTTP 200 + success:true + system_name:TokenKi |
| 冒烟测试 | ⚠️ 跳过（高风险，用户二次显式授权） | 预配置 smoke token 0 可用模型，无法取得真实 200；详见下方 |
| 部署后监控 | ✅ | ~7h 全窗口无崩溃类 5xx，0 panic，0 重启 |

## 部署详情
| Deployment | 旧镜像 | 新镜像 | rollout |
|-----------|--------|--------|---------|
| tokenki-api-master | v2026.06.16.3 | v2026.06.17.1 | ✅ successfully rolled out |
| tokenki-api-slave  | v2026.06.16.3 | v2026.06.17.1 | ✅ successfully rolled out (2/2) |

## 验证
| 检查项 | 结果 |
|--------|------|
| Pod 全 Running | ✅ 3/3（master×1 + slave×2），RESTARTS=0，连续运行 ~7h |
| 镜像 tag 已更新 | ✅ master + slave 均 v2026.06.17.1 |
| 日志无 panic | ✅ master/slave 近 500 行无 panic/nil pointer/runtime error |
| API 冒烟（真实 200） | ⚠️ 跳过 —— smoke token 无可用渠道，平台窗口内无真实 relay 流量 |

## 冒烟测试跳过说明（残余风险）
- 预配置环境变量 `TOKENKI_SMOKE_API_KEY` 当前 **0 可用模型**（`/v1/models` 返回空，其 default 组未映射启用渠道），`/v1/chat/completions`、`/v1/messages` 均返回结构化 503「No available channel」（带 request_id，非崩溃）。
- 本次为供应商后台改动（2FA/结算/日志），不触碰 relay 成功路径代码。
- 用户经**两轮显式确认**接受跳过冒烟测试的全部风险。
- **残余未验证项**：relay 成功完成路径（真实 200）在 ~7h 窗口内既无真实流量、也无我的成功请求，未被实时验证。其余健康信号全绿。

## 部署后监控报告（~7h 全窗口，固定 FROM=1781636534000）
> 注：因 Phase 4 两次 AskUserQuestion 等待跨越真实世界 ~7h，原「3 分钟」窗口实际拉长为 ~7h，证据更充分。

| 状态码 | 来源/路径 | 次数 | 与本次变更相关 |
|--------|-----------|------|---------------|
| 200 | 非 relay（/api、web、dashboard、/v1/models 等） | 152 | ✅ 无关（应用正常服务） |
| 429 | 漏洞扫描机器人探测 .env / phpinfo.php | 50 | ✅ 无关（限流正确挡掉，互联网背景噪音） |
| 404 | 扫描 .env（/api/.env、/v1/.env 等） | 5 | ✅ 无关 |
| 503 | /v1/chat/completions、/v1/messages | 2 | ✅ 无关（我的冒烟测试，token 无渠道） |

总计 4xx/5xx：57（429:50，404:5，503:2）· **崩溃类 5xx（500/502/504）：0**
结论：**正常，无版本引入问题。**

## 回滚记录
无。

## 经验记录（SOP 改进点）
1. **P6 监控脚本 macOS 兼容 bug**：临时脚本用了 `timeout 25 tccli ...`，但 macOS 默认无 `timeout` 命令，导致 4 轮全部 QUERYERR（空输入）。已改用前台无 timeout 查询绕开。skill SOP 模板本身不含 `timeout`，是本次临时脚本自加 —— 后续若需超时保护应用 `gtimeout`（coreutils）或省略。
2. **长等待使 P6 时间窗失真**：AskUserQuestion 阻塞期间真实世界可跨小时，部署时刻记录的固定 FROM 到监控实跑时已过 7h。本次反而获得更长证据窗口（净正向），但说明「3 分钟窗口」在有人工确认介入时不可机械套用。
