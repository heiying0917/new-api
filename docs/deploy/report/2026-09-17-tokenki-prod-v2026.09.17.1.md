# 发布报告 — tokenki v2026.09.17.1

> 环境:prod · 集群:cls-tokensolo-tke-japan / ns tokenki · 时间:2026-09-17 13:18 (UTC+8)

## 概览
| 项 | 值 |
|----|----|
| 服务 | tokenki |
| 环境 | prod |
| 版本 tag | v2026.09.17.1 |
| 镜像 | ghcr.io/heiying0917/tokenki:v2026.09.17.1 |
| 发布结果 | ⚠️ 已回滚（部署 22.5 分钟后因渠道创建 22P02 回滚到 v2026.06.24.1，见回滚记录） |
| 本次内容 | 上游必要修复同步(26 提交)：GHSA-8r8v 计费整数溢出、GHSA-j6gc Redis 额度缓存覆盖两条安全链；优雅关闭、ResponseHeaderTimeout 防 OOM、PG PrepareStmt=false、SQLite WAL、HTTP/2 GetBody 重试、Bedrock 断开取消、Claude 空 tools；GPT-5.5/5.6/6-astra 与 Claude 5 系(opus-5/sonnet-5/fable-5.1/mythos)默认定价、内置列表、Bedrock/Vertex ID 映射、adaptive-thinking 能力表；Realtime GA、gemini-3 image GA |

## 各阶段结果
| 阶段 | 状态 | 详情 |
|------|------|------|
| 合并 main | ✅ | `chore/upstream-sync-2026-09` → main fast-forward(26 提交)，另补 `429d231e1 docs(deploy)` 收旧 v2026.06.24.1 发版报告 |
| 预检 | ✅ | `go build ./...` 通过；`go test ./model/... ./service/... ./controller/... ./logger/...` 仅白名单 `TestListModelsTokenLimitIncludesTieredBillingModel` 失败，无白名单外失败 |
| 打 tag & push | ✅ | main `7985add51..429d231e1` push；tag v2026.09.17.1 → 429d231e1 |
| 镜像构建 (Actions) | ✅ | run-id 35184741435 `Publish Docker image (GHCR for TKE)`，`gh run watch` exit 0 + JSON 二次校验 completed/success，耗时约 5 分钟；GHCR manifest linux/amd64 就位 |
| 集群部署 | ✅ | master×1 → slave×2 顺序滚动，均 successfully rolled out，零停机；部署完成 05:17:51Z |
| 健康检查 | ✅ | https://tokenki.com/api/status → HTTP 200，success:true，system_name=TokenKi；集群内 `kubectl exec` 直连 Pod 127.0.0.1:3000 亦 200 |
| 冒烟测试 | ✅ | 见下表；0 panic |
| 部署后监控 | ✅ | P6 六轮(05:21:44Z–05:24:28Z 累计窗口)真实用户 5xx = 0 |

## 部署详情
| Deployment | 旧镜像 | 新镜像 | rollout |
|-----------|--------|--------|---------|
| tokenki-api-master | v2026.06.24.1 | v2026.09.17.1 | ✅ successfully rolled out |
| tokenki-api-slave  | v2026.06.24.1 | v2026.09.17.1 | ✅ successfully rolled out |

## 验证
| 检查项 | 结果 |
|--------|------|
| Pod 全 Running | ✅ master 1/1 + slave 2/2，0 重启；Service `tokenki-api` selector role=slave，EndpointSlice 172.16.1.16/172.16.1.79 = 两个新 slave Pod |
| 镜像 tag 已更新 | ✅ 两 deployment 均 v2026.09.17.1 |
| 日志无 panic | ✅ master/slave `--since=4m` panic/nil pointer/runtime error = 0；master error 级 = 0；slave error 级 8 条 = 本次冒烟 8 次 model_not_found(user_id=2) |
| 数据库迁移 | ✅ master 启动日志 `database migration started`，无 error；PG `PrepareStmt=false` 生效后查询正常 |
| 本机网络抖动 | ⚠️ P4 后本机对 tokenki.com 一次 SSL_ERROR_SYSCALL + 一次超时、tccli 一次 ClientNetworkError；随即公网健康 3 连 200(1.1–1.3s)、集群内直连 200、第三方站点均 200，判定为本机出口瞬时抖动，非服务异常 |

## 冒烟测试报告
| 场景 | 请求模型/接口 | 预期 | 实际响应 | HTTP | 结论 |
|------|-------------|------|---------|------|------|
| 健康检查 | GET /api/status | 200 success:true | success:true, system_name=TokenKi | 200 | ✅ |
| 基线 OpenAI chat | gpt-4o-mini | 200 或结构化 model_not_found | model_not_found(distributor, group default 无渠道) | 503 | ✅ 非崩溃，与历次一致 |
| 基线 Anthropic | /v1/messages claude-sonnet-4-6 | 同上 | model_not_found | 503 | ✅ |
| 本次: Claude 5 effort 后缀 | claude-opus-5-high (chat) | 结构化响应 | model_not_found，模型名被完整识别透传 | 503 | ✅ 无 panic |
| 本次: Claude 5 thinking 后缀 | claude-sonnet-5-thinking (/v1/messages) | 结构化响应 | model_not_found | 503 | ✅ |
| 本次: GHSA-8r8v 图片 n 溢出 | /v1/images/generations n=18446744073686646784 | 4xx 参数错误 | model_not_found(distributor 先于校验) | 503 | ⚠️ 未触达校验层，见未覆盖 |
| 本次: 图片 n=0 | /v1/images/generations | 4xx | model_not_found | 503 | ⚠️ 同上 |
| 本次: 视频 duration 溢出 | /v1/video/generations duration=9999999999 | 4xx | model_not_found | 503 | ⚠️ 同上 |
| 本次: 新模型定价 | gpt-5.6-sol (chat) | 结构化响应 | model_not_found | 503 | ✅ |

**未覆盖场景**：
- GHSA-8r8v 的数量参数校验与饱和转换、Claude 5 adaptive-thinking 请求改写、cache 计费——冒烟 token(user_id=2) 的 default 组无任何渠道，请求在 distributor 层即返回，无法进入 relay 校验/转换/结算路径。已由分支上的单元测试(quota_math、saturation、price、Claude5×3、latest_models 等)与本地 PG 真库两次启动验证覆盖。
- GHSA-j6gc(PUT /api/user/self)需要控制台登录会话，relay API key 返回 401，无法用冒烟 token 触发；由 `model/user_update_test.go` 覆盖。
- 若需生产端到端验证，可给冒烟 token 所在组配置任一可用渠道后重跑上表。

## 部署后监控(P6，3 分钟)
- 监控起点 FROM=1789622271000(05:17:51Z，部署完成时刻)，累计窗口 tccli SQL 聚合 CLS(ap-tokyo, topic tke_tokenki)。
- 轮次 1–6(05:21:44Z–05:24:28Z)：status>=400 恒为 `503×8`，明细全部 user_id=2(冒烟 token)：/v1/chat/completions×3、/v1/images/generations×2、/v1/messages×2、/v1/video/generations×1；累计 access 10→11 条(低流量时段)。
- 发版前 1 小时同长度窗口：5xx = 0 → 本次 8 条 503 全部为冒烟自测，**真实用户 5xx 全程 = 0**。
- 结论：发版健康，无版本引入的异常，无需回滚。

## 上线后注意
- 新增环境变量 `RELAY_RESPONSE_HEADER_TIMEOUT`(默认 1800 秒)：上游响应头等待上限，防止卡死连接导致内存持续增长；设 0 恢复旧行为。
- relay 对上游 3xx 不再跟随跳转、原样返回(随上游 GetBody 修复引入)。
- PostgreSQL 连接已关闭 GORM PrepareStmt(兼容连接池代理)；gorm 保持 1.25.2 未升级。
- 首启 48 条 `ALTER COLUMN` 归一化 DDL 为预先存在行为(与 v2026.06.24.1 相同)，非本次引入。

## 回滚记录
- **13:35–13:37** 供应商新建渠道报 `ERROR: invalid input syntax for type json (SQLSTATE 22P02)`，CLS 8 条 error 级 `INSERT INTO "channels"`。
- **13:39–13:40:26** 按红线先止血：`kubectl rollout undo` slave → master，两 deployment 回到 v2026.06.24.1，健康 200。
- 根因：cherry-pick 上游 `66031a09d`（PG 关闭 PrepareStmt → pgx simple protocol）时漏拿同日配套修复 `6eb6f35ed`（json 列 Valuer 须返回 string，否则 []byte 按 bytea 发送）。P1/P3/P4 均未触达 PG 写 json 列路径。
- 复盘与改进：`docs/report/2026-09-17-pg-json-column-22p02.md`；修复后重发 v2026.09.17.2。
